<%= form_for(@log) do |f| %>
  <% if @log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@log.errors.count, "error") %> prohibited this log from being saved:</h2>

      <ul>
      <% @log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Right now, I am..." %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label "Tell me more..." %><br>
    <%= f.text_area :entry %>
  </div>
  <div class="field">
    <%= f.label "What is this really about?..." %><br>
    <%= f.text_area :tags %>
  </div>
  <div class="field">
    <%= f.label "Show me..." %><br>
    <%= f.file_field :image %>
  </div>
    <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
